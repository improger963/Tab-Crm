# TabCRM works fully offline with localStorage as its database.
# No API keys or environment variables are required at the moment.
# (GEMINI_API_KEY / APP_URL were leftovers from the AI Studio template and
#  the removed Google Sheets / Gemini integrations — intentionally dropped.)
